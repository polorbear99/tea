# 网页前端改造方案

> 目标：小程序代码冻结不动，新建独立网页前端目录，复用现有 NestJS 后端。
> 版本：v2.0 ｜ 已并入三项决策：① 支付走方案 A ② 支持 H5 移动端 ③ 数据库改用 PostgreSQL

---

## 一、已拍板决策

| 决策项 | 结论 | 影响 |
|---|---|---|
| **支付** | **方案 A**：线下支付 / 货到付款占位 | 绕开服务号资质门槛，订单流程完整跑通，支付位预留 |
| **前端形态** | **H5 + 移动端适配** | 手机浏览器 / 微信内直接打开，响应式 mobile-first |
| **数据库** | **PostgreSQL**（脱离 Supabase） | 改用 Drizzle ORM 直连，可生成 migration、支持事务 |

---

## 二、核心结论

**后端几乎不用改，数据库层是唯一需要动手术的地方。**

现有后端已开启 `app.enableCors({ origin: true, credentials: true })`（`server/src/main.ts:19-22`），全局前缀 `/api`。网页端直接跨域调用即可。

但数据库层现状是：`schema.ts` **已经用 Drizzle 定义好了 16 张表**，可运行时却走 `@supabase/supabase-js` 客户端（`supabase-client.ts`），Drizzle 的 schema 只当文档用，一次都没执行过。这导致三个后果：

1. 全库**无建表 SQL、无 migration、无 `drizzle.config`** —— 表得手动建，现在跑不起来
2. 下单扣库存**无事务**（`orders.service.ts:83-91` 循环里逐条 update），中途失败数据就脏了
3. 依赖 Supabase 云服务，部署受制于人

**改用 PostgreSQL + Drizzle 直连，这三个问题一次性解决。**

---

## 三、目录结构

```
茶叶小程序/
├── src/                 ← 【冻结】小程序端，本方案不碰
├── server/              ← 【改造】NestJS 后端
│   ├── drizzle.config.ts      ← 新增
│   └── src/
│       ├── storage/database/
│       │   ├── shared/schema.ts      ← 已有，直接用（16 表）
│       │   ├── db.ts                 ← 新增：Drizzle + pg 连接池
│       │   └── supabase-client.ts    ← 废弃移除
│       ├── products/  orders/  ai/   ← 重写查询层（Supabase 链 → Drizzle）
│       └── admin/                    ← 新增模块
├── web/                 ← 【新增】网页前端（H5 + 移动端）
│   ├── index.html            ← viewport + 微信适配 meta
│   ├── package.json
│   ├── vite.config.ts        ← host: true（手机可局域网访问）
│   ├── tailwind.config.js    ← mobile-first 断点 + 设计 token
│   └── src/
│       ├── main.tsx
│       ├── App.tsx
│       ├── api/              后端接口封装
│       ├── components/
│       │   ├── ui/           Button / Card / Dialog / Tabs / Badge / Toast
│       │   └── business/     商品卡、茶品信息面板
│       ├── pages/
│       │   ├── c/            消费者端
│       │   └── admin/        茶商后台（桌面为主）
│       ├── hooks/            useMediaQuery、useWechat
│       └── styles/           design token + 安全区适配
└── pnpm-workspace.yaml        ← 加 'web'
```

**隔离保证**：`web/` 是独立 npm 包，与 `src/` 零依赖、零引用。改网页不影响小程序编译。

---

## 四、技术选型

| 层 | 选型 | 说明 |
|---|---|---|
| 框架 | React 18 + TypeScript | 与小程序同栈，逻辑可平移 |
| 构建 | Vite 5 | 快，且 `host: true` 手机可直连调试 |
| 样式 | Tailwind CSS | `design_guidelines.md` 已是 Tailwind 语义，色板直接搬 |
| 路由 | React Router 6 | H5 需要 URL 可分享、可扫码直达 |
| 数据请求 | TanStack Query | 缓存、重试、loading 开箱即用 |
| 状态 | Zustand | 轻量 |
| 图标 | lucide-react | 现用 `lucide-react-taro`，换 web 版 |
| ORM | **Drizzle + node-postgres** | 替换 Supabase 客户端 |

---

## 五、H5 移动端适配方案

### 5.1 布局策略：mobile-first，桌面增强

- 基准按手机设计（`sm:640 md:768 lg:1024`），桌面端再放大留白
- C 端用**底部 TabBar**（4 项：首页 / 茶品 / AI 问茶 / 我的），对齐小程序心智
- 茶商后台以**桌面为主**（表格密集），但保证手机能应急查看和发货

### 5.2 必须处理的移动端细节

| 项 | 处理 |
|---|---|
| viewport | `width=device-width, initial-scale=1, viewport-fit=cover` |
| iPhone 安全区 | `env(safe-area-inset-bottom)` 处理底部 TabBar 与刘海 |
| 点击态 | 所有可点元素加 `:active` 反馈，避免 iOS 无 hover 导致"点了没反应" |
| 输入框 | `font-size ≥ 16px`，否则 iOS 聚焦时自动放大页面 |
| 图片上传 | `<input type="file" accept="image/*" capture="environment">` 直接调起摄像头 |
| 滚动 | `-webkit-overflow-scrolling: touch` |

### 5.3 微信内置浏览器适配（重点）

H5 主要在微信里被打开，有三个坑：

1. **扫码**：`getUserMedia` 在 iOS 微信内**不可用**。方案：优先接微信 JS-SDK `wx.scanQRCode`（需服务号 + 备案域名），**兜底用"上传照片识茶"**。
   → 因此把「上传照片识茶」做成主入口，扫码做次入口。这反而更贴合 PRD 5.3.3，且网页端能选相册原图，识别效果比小程序好。
2. **分享**：接 JS-SDK 自定义分享标题/缩略图，否则分享出去是裸链接。
3. **支付**：已决定走方案 A（线下支付），**不依赖 JSAPI，无需服务号即可完整跑通** —— 这是选 A 的隐性收益。

### 5.4 二维码落地页

`https://域名/t/:code` → 普通二维码，微信直接可扫，印刷成本低。
`code` 用现成 `biz_id`（UUID），天然满足"不暴露自增 ID"。商品下架后仍可打开，只隐藏购买按钮（PRD 5.3.2）。

---

## 六、数据库改造方案（PostgreSQL + Drizzle）

### 6.1 现状与改造量

| 文件 | 现状 | 改造 |
|---|---|---|
| `shared/schema.ts` | Drizzle 定义 16 表，**已完成** | **不动，直接用** |
| `supabase-client.ts` | Supabase 客户端 | 删除 |
| `db.ts` | 无 | 新增：Drizzle + pg 连接池（约 20 行） |
| `drizzle.config.ts` | 无 | 新增（约 10 行） |
| 6 个 service | Supabase 链式 API | **重写查询**（主要工作量） |

需要重写查询的文件：`orders.service.ts`、`products.service.ts`、`ai.service.ts`、`categories.service.ts`、`customers.service.ts`、`shop.service.ts`。
其中 `orders.service.ts` 最复杂（3 个方法，含联表、聚合、分页），其余较简单。

### 6.2 改造后直接获得的三个收益

1. **能生成建表脚本了** —— `drizzle-kit generate` 基于现成 schema 自动生成 SQL，`drizzle-kit migrate` 执行，彻底解决"跑不起来"
2. **支持事务** —— 下单逻辑改成 `db.transaction()`，库存扣减与订单创建同生共死，顺手修掉现存的脏数据风险
3. **部署自由** —— 本地 Docker、自建、云数据库（阿里云 RDS / 腾讯云 TencentDB）均可

### 6.3 环境要求

- PostgreSQL **14+**（推荐 16）
- 本地开发：`docker run -d --name tea-pg -e POSTGRES_PASSWORD=xxx -p 5432:5432 postgres:16`
- 连接串：`DATABASE_URL=postgresql://user:pass@localhost:5432/tea_shop`
- `pg` 依赖已在 `server/package.json` 中（无需新装）

### 6.4 兼容性

全部兼容。唯一确认点：`gen_random_uuid()` 需 PG 13+（内置）；更低版本需 `CREATE EXTENSION pgcrypto`。

---

## 七、后端需要新增的接口（只增不改）

现有 17 个接口全部保留，新增：

| 模块 | 新增接口 | 优先级 |
|---|---|---|
| admin | `POST /api/admin/login`（账号密码 + JWT） | P0 |
| products | `POST /` `PUT /:id` `DELETE /:id` `PATCH /:id/status` | P0 |
| products | `POST /upload`（图片上传） | P1 |
| orders | `PATCH /:id/status`（确认/发货/完成/取消/退款） | P1 |
| orders | `POST /:id/pay` —— **方案 A：标记线下收款/货到付款** | P1 |
| shop | `PUT /config` | P2 |
| ai | `PUT /knowledge` | P2 |

**注意：后端目前全局无任何鉴权**（Guard 命中为 0）。新增 admin 模块时需一并补上 JWT 守卫。

---

## 八、支付方案 A 的具体设计

不接支付网关，但**订单状态机完整**，保证后续能平滑升级到真实支付。

```text
待付款 pending
  ↓ 用户提交订单（线下转账 / 货到付款）
待发货 paid          ← 由「用户标记已付款」或「茶商后台确认收款」触发
  ↓ 茶商发货（填快递公司 + 单号）
已发货 shipped
  ↓ 用户确认收货 / 超时自动
已完成 completed

任意前置状态 → 已取消 cancelled / 已退款 refunded
```

关键设计：
- 订单表**保留 `payment_method` 字段**（`offline` / `cod` / `wechat`），未来接微信支付只需增加 `wechat` 分支
- 收货信息页展示**收款二维码 / 银行账号**（后台可配置），用户线下转账后点"我已付款"
- 茶商后台可手动"确认收款"，避免因用户忘点导致卡单
- 库存改为**支付后扣减**（现在是不管付没付先扣，会永久占库存）

---

## 九、分期计划

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P0 地基** | 数据库切 PG + Drizzle migration；`web/` 骨架 + 设计 token + API 封装；后台登录 | 项目能跑起来，能连数据库 |
| **P1 C 端 H5** | 首页 / 商品 / 详情 / AI 问茶 / 二维码落地页（移动端适配） | 手机可完整浏览 |
| **P2 交易闭环** | 下单 / 订单列表 / 线下支付状态机 / 地址管理 | 能真实成交 |
| **P3 茶商后台** | 工作台 / 商品管理 / 二维码生成 / 订单发货 / 店铺设置 | 茶商能自主运营 |
| **P4 补齐** | 照片识茶 / AI 选茶 / 冲泡指导 / 茶宠 | PRD 一期全覆盖 |

---

## 十、剩余待拍板（2 项）

1. **本轮开工范围**：P0 + P1（地基 + C 端 H5）先做，还是 P0～P3（含茶商后台）一起？
   - 建议先 P0+P1，你能最快在手机上看到东西；后台是体力活，可并行。
2. **PostgreSQL 部署形态**：本地 Docker（开发快）／ 云数据库（直接可上线）／ 你已有现成实例？

技术栈如无异议即按第四节执行。
